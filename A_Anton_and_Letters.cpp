#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin>>s;
    cout<<s;
    // set<char>st;
    // for(int i=1;i<s.length()-1;i++){
    //     st.insert(s[i]);
    // }
    // for (auto it = st.begin(); it != st.end(); ++it)
    // cout << *it << " ";
}
